import LoadingDisplay from '@/components/Layout/Loading/LoadingDisplay';

export default function Loading() {
  return (
    <LoadingDisplay
      title={`Branches`}
      message={`the Velocitor Helpdesk Branches List`}
    />
  );
}
